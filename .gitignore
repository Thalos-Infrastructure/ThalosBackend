node_modules
dist
.env
.env.local
.env.*.local
*.log

# Never commit pulled env dumps or temp tooling files (may contain secrets)
.tmp-*
.tmp-fe-pull/
prod*.env
*.env.pulled

# Local working clones of other repos (must never be nested into this repo)
frontend-fix/
*-fix/

# Permitir subir el archivo de ejemplo (solo placeholders, sin secretos)
!.env.example
.vercel
.env*
